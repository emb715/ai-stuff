---
title: "Weekly Changelog - 2026-26"
status: draft
confidence: medium
last_tested: 2026-06-25
scope: personal
tooling:
  - "opencode/claude-sonnet-4-6"
tags:
  - changelog
  - weekly
owner: "@emb715"
---

# Wins

- `prompt-factory` experiment promoted to canonical skill at `skills/prompt-factory/`
- Interactive guided flow implemented: plan gate → combined type+shape menu → generate in one interaction
- All 5 prompt types defined with full output contracts: `implementation`, `handoff`, `review`, `debug`, `readiness-check`
- `strict` shape validated and enabled — tested against `shareable-template-plan.md`, produced correct phased checkpoint structure
- All 3 implementation shapes (`default`, `fast`, `strict`) live and tested
- DL011 lint gate added: non-experiment runtime artifacts blocked from referencing `experiments/` as runtime dependencies
- `skills/` and `prompts/` added to lint target scope
- Command renamed from `/prompt-gen` to `/prompt-factory` — name now matches artifact purpose
- Official OpenCode + Claude Code command docs fetched and saved to `docs/references/commands/`
- Two prompt invocation modes (copy/paste and command) documented in `prompts/README.md`
- Self-contained runtime rule codified in `docs/standards/artifact-structure.md`

# What failed

- `prompts/prompt-factory/` created and immediately removed — prompt artifact format doesn't work for multi-mode branching logic; skill is the correct destination
- `balanced/compressed` style gate was invented without prior history; caught and removed before shipping

# What changed in process

- Artifact structure rule updated: minimum three files, not exactly three — additional files/folders allowed
- New rule: non-experiment artifacts must be runtime self-contained (no `experiments/` runtime dependencies)
- Command wrappers now live inside the artifact folder they belong to (`skills/<name>/commands/`)
- `prompts/README.md` updated: dual invocation modes are now a documented standard

# Promotions

- draft → validated: `skills/prompt-factory/` — all three implementation shapes tested live; full type system defined with contracts; strict mode unblocked

# Deprecations

- `experiments/prompt-factory/commands/prompt-gen.md` — superseded by `skills/prompt-factory/commands/prompt-factory.md`

# Focus for next week

- Run remaining prompt types (`handoff`, `review`, `debug`, `readiness-check`) in live sessions and record evidence
- Evaluate `skills/prompt-factory/` for `validated → vetted` after non-implementation types have at least one real run each
- Execute `shareable-template-plan.md` using the `strict` prompt generated this session
